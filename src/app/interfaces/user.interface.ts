export interface IUser {
  username: string;
  name: string;
  repoCount: number;
  followerCount: number;
  avatarUrl: string;
  bio: string;
  company: string;
  location: string
  followingCount: number;
}
