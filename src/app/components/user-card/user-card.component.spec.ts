import {
  TestBed
} from '@angular/core/testing';

import {
  UserCardComponent
} from './user-card.component';

describe('UserCardComponent', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [
        UserCardComponent
      ]
    }).compileComponents();
  });

  it('should spawn the user profile dialog if "More Info" is clicked', () => {

  });

  it('should show the description if it exists', () => {

  });

  it('should not show the description if it does not exist', () => {

  });
});
