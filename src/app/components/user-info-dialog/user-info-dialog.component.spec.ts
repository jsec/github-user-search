import {
  TestBed
} from '@angular/core/testing';

import {
  UserInfoDialogComponent
} from './user-info-dialog.component';

describe('UserInfoDialogComponent', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [
        UserInfoDialogComponent
      ]
    });
  });

  it('should close the dialogRef if the close button is clicked', () => {

  });

  it(`should navigate to the user's GitHub profile if Go to Profile is clicked`, () => {

  });
});
